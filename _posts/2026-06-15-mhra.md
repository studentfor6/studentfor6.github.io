---
layout: post
title: "Markdown Parsing Test"
date: 2026-06-15
description: "Test file to exercise Markdown vs HTML parsing"
---

<!-- Inline HTML block before a Markdown heading (tests block HTML handling) -->
<div class="test-html-wrapper">
  <p>This paragraph is inside a raw HTML block. Markdown block syntax that follows may be treated as raw text if the HTML is left unclosed.</p>
</div>

## Purpose

This short lead paragraph verifies that a Markdown H2 renders correctly when not inside an HTML block.

## **Bold Heading Test**

This heading intentionally uses bold markers inside the heading to test fragility: the line above should render as an H2 with bold text.

### Inline HTML heading (explicit)

<h3 class="text-2xl">This is an HTML &lt;h3&gt; heading</h3>

This paragraph follows an explicit HTML heading and should render normally.

## Table rendering (HTML table with plain th)

<div class="overflow-x-auto">
<table role="table" aria-label="Test table" class="min-w-full">
  <thead>
    <tr>
      <th scope="col" class="bg-[#0f4d8a] text-white px-4 py-2">Column</th>
      <th scope="col" class="bg-[#0f4d8a] text-white px-4 py-2">Value</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td class="px-4 py-2">Row 1</td>
      <td class="px-4 py-2">Alpha</td>
    </tr>
    <tr>
      <td class="px-4 py-2">Row 2</td>
      <td class="px-4 py-2">Beta</td>
    </tr>
  </tbody>
</table>
</div>

## Markdown table (GFM)

| Feature | Expected result |
|---|---|
| `## Heading` at column 1 | Renders as `<h2>` |
| `## **Heading**` | Renders as `<h2>` with bold text (may be fragile) |
| HTML block left unclosed | Subsequent Markdown block syntax may be ignored |

## Code block

```js
// This is a fenced code block; should render as code
console.log("Markdown parser test");
